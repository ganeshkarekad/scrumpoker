{% extends 'page.html.twig' %}

{% block title %}Room {{ room.roomKey }} - {{ constant('App\\Constant\\Site::NAME') }}{% endblock %}

{% block content %}
    <div {{ react_component('Dashboard', {
        'roomKey': room.roomKey,
        'currentUserId': user.userLoginId,
        'mercureUrl': mercureUrl
    })|raw }}>
    </div>

{% endblock %}
